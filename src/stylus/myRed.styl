@import './base'

.scoreDetail
  padding 0px 10px
  height 40px
  line-height 40px
  background-color #fff
  border-bottom solid 1px #f2f2f2
  font-size 12px
  & > div
    &:nth-child(odd)
      margin-left 20px
    &:nth-child(even)
      margin-right 20px
